# Les paquets @team-papers viennent du registre GitHub, pas de npmjs.
# Le jeton n'est jamais ecrit ici : il est fourni par l'environnement
# (NODE_AUTH_TOKEN) au moment de l'installation.
@team-papers:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
